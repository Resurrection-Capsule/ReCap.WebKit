// ReCap.WebKit clean-room stub for WebKit's <wtf/FastAllocBase.h>.
// EAWebKitSTLWrapper.cpp only #includes it; it references no symbol from it. Empty stub keeps the
// wrapper TU WebCore-free (the real header pulls WTF's fastMalloc allocator).
#pragma once
